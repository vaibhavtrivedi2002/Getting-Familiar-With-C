//this program shows use if conditional statement if-else
#include<stdio.h>
int main (){
    int num;
    printf("Enter a number:");
    scanf("%d",&num);
if (num<0){
  printf("negative",&num); 
}else{
    printf("positive",&num);
}
    return 0;
}